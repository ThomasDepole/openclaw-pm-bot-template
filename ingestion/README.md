# Ingestion Drop Zone

Drop files here to be processed by the PM agent:

- **Flat files** (PDFs, Word docs, spreadsheets, PowerPoints) → drop directly in this folder
- **Meeting notes** → drop in `meetings/`
- **Calendar screenshots** → drop in `calendar/`

Files are processed on request and deleted after ingestion. The `memory/` folder is the permanent record.
